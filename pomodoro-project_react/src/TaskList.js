import React, { Component } from "react";

const TaskList = () => {
    return (
        <section className="TaskList main-section">
            Task List component
            <button>Test</button>
            <button>Test</button>
        </section>
    );
};

export default TaskList;
